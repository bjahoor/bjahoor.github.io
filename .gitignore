# build output
dist/
.astro/

# dependencies
node_modules/

# LaTeX intermediates — the PDF itself is committed, the scratch files are not
resume/*.aux
resume/*.fdb_latexmk
resume/*.fls
resume/*.log
resume/*.out
resume/*.synctex.gz
resume/build.log

# environment
.env
.env.production

# macOS / editor cruft
.DS_Store
resume/status.json
